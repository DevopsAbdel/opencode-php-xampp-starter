# opencode-php-xampp-starter
If this repo is meant to hold only these two markdown files (the guide + the kickoff prompt) as a reusable reference you copy into every new project, I'd go with opencode-php-xampp-starter — short, lowercase, hyphenated (good Git convention), and immediately tells anyone what it's for.
